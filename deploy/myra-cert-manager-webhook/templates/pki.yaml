---
# Create a selfsigned Issuer, in order to create a root CA certificate for
# signing webhook serving certificates
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "myra-cert-manager-webhook.selfSignedIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "myra-cert-manager-webhook.name" . }}
    chart: {{ include "myra-cert-manager-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selfSigned: {}

---

# Generate a CA Certificate used to sign certificates for the webhook
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "myra-cert-manager-webhook.rootCACertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "myra-cert-manager-webhook.name" . }}
    chart: {{ include "myra-cert-manager-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "myra-cert-manager-webhook.rootCACertificate" . }}
  duration: 43800h # 5y
  issuerRef:
    name: {{ include "myra-cert-manager-webhook.selfSignedIssuer" . }}
  commonName: {{ printf "ca.%s.cert-manager" (include "myra-cert-manager-webhook.fullname" .) | quote }}
  isCA: true

---

# Create an Issuer that uses the above generated CA certificate to issue certs
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "myra-cert-manager-webhook.rootCAIssuer" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "myra-cert-manager-webhook.name" . }}
    chart: {{ include "myra-cert-manager-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ca:
    secretName: {{ include "myra-cert-manager-webhook.rootCACertificate" . }}

---

# Finally, generate a serving certificate for the webhook to use
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "myra-cert-manager-webhook.servingCertificate" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: {{ include "myra-cert-manager-webhook.name" . }}
    chart: {{ include "myra-cert-manager-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  secretName: {{ include "myra-cert-manager-webhook.servingCertificate" . }}
  duration: 8760h # 1y
  issuerRef:
    name: {{ include "myra-cert-manager-webhook.rootCAIssuer" . }}
  dnsNames:
  - {{ include "myra-cert-manager-webhook.fullname" . }}
  - {{ include "myra-cert-manager-webhook.fullname" . }}.{{ .Release.Namespace }}
  - {{ include "myra-cert-manager-webhook.fullname" . }}.{{ .Release.Namespace }}.svc
